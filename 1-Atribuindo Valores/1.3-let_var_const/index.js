/* 
não hoisting = Necessário pelo menos declarar a váriavel antes de utilizar
var{ Escopo = global ou local; Pode redeclarar = sim; pode reatribir = sim; hoisting = sim }  
const{ Escopo = bloco; Pode redeclarar = não; pode reatribir = não; hoisting = não }
let{ Escopo = bloco; Pode redeclarar = não; pode reatribir = sim; hoisting = não }
*/