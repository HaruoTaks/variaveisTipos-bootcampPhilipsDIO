/* let um = "um";
console.log(um); */

// String 
let nome = "João";
let sobrenome = "Pedro";

// Concatenado
//let concatenado = nome.concat(sobrenome);

// Concatenado com espaço
// let concatenado = nome + " " + sobrenome;

// Concatenado pulando linha
// let concatenado = nome + "\n" + sobrenome;

// Concatenado com espaço nova forma
let concatenado = `${nome} ${sobrenome}`;
console.log(concatenado);

/* let letra = nome[1];
console.log(letra); */